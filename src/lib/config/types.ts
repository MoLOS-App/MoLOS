/**
 * Standardized types for module configuration
 *
 * Re-exported from @molos/module-types for backward compatibility.
 * New code should import directly from @molos/module-types.
 */
export { type ModuleConfig, type NavItem, type IconComponent } from '@molos/module-types';
