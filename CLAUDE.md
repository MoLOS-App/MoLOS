# Claude Code Instructions

## Git Workflow

- **Do NOT push or commit anything** unless explicitly asked by the user
- When committing is requested:
  - Use simple commit messages
  - Do NOT add any additional authors (no `Co-Authored-By` lines)
  - Just use the default author configured in git
