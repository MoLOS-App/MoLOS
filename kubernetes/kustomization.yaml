apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: molos
  app.kubernetes.io/part-of: molos-system

images:
  - name: ghcr.io/your-username/molos
    newTag: latest
